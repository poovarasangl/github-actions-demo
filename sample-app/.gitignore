# The build output is produced by `npm run build` and uploaded as a CI
# artifact (Day 2). It is NEVER committed — that is the whole point of a
# build step: the pipeline regenerates it from source on every run.
dist/

# No dependencies today, but this is the habit to keep.
node_modules/

# Test reports written by the matrix demo (Day 3, file 26) before they are
# uploaded as artifacts. Generated per run — never committed.
reports/
